## Screencap & compare pages on 2 different sites
### To run
`yarn install`
`yarn run capture`

Screenshots will be in `/screenshots/<page>-<sitename>.png`.

#### For more info see `tests/capture-site.js`

